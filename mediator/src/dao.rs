// data access object

use crate::{User, School};

pub struct UserDao{}

impl UserDao{
    fn query_user_info_by_id(id: u64)->User{
        
    }
}

pub struct SchoolDao{
    fn query_school_info_by_id(id: u64)->School{
    }
}